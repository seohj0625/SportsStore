@using System.Globalization
@model Domain.Entities.Product

<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-primary">@Model.Price.ToString("c", new CultureInfo("en-US", false).NumberFormat)</span>
    </h3>
    <span class="lead">@Model.Description</span>
</div>